#include "../Utilities/utilities.cpp"

using namespace utilities;

namespace generate{

    figure createPlane(float, int);
    figure createBox(float, int);
    figure createSphere(float, int, int);
    figure createCone(float, float, int, int);
}
